{
  "name": "ns-arena",
  "version": "1.2.1-beta",
  "description": "NS Arena BETA-1.0.0 — Cyberpunk Multiplayer Combat",
  "private": true,
  "scripts": {
    "sync": "cap sync android",
    "build:apk": "cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/splash-screen": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
